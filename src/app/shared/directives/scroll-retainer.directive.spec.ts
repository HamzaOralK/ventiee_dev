import { ScrollRetainerDirective } from './scroll-retainer.directive';

describe('ScrollRetainerDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollRetainerDirective();
    expect(directive).toBeTruthy();
  });
});
