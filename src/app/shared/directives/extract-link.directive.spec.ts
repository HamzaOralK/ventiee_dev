import { ExtractLinkDirective } from './extract-link.directive';

describe('ExtractLinkDirective', () => {
  it('should create an instance', () => {
    const directive = new ExtractLinkDirective();
    expect(directive).toBeTruthy();
  });
});
